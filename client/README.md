# Fuse Client

Клиентское десктопное приложение Fuse на Electron + React.

## Быстрый старт

### Установка

```bash
npm install
```

### Запуск

Разработка:
```bash
npm run dev
```

Продакшн (упакованное приложение):
```bash
npm start
```

### Сборка

Windows:
```bash
npm run build
npm run build:win
```

macOS:
```bash
npm run build
npm run build:mac
```

Linux:
```bash
npm run build
npm run build:linux
```

Собранные приложения появятся в папке `release/`.

## Использование

### 1. Подключение к серверу

При первом запуске вам нужно подключиться к серверу:

1. Введите IP адрес сервера (например, `192.168.1.100` или `example.com`)
2. Введите порт (по умолчанию `3000`)
3. Нажмите "Подключиться"

### 2. Регистрация/Вход

После успешного подключения:

- **Регистрация**: Введите username, email и пароль
- **Вход**: Введите username и пароль

### 3. Работа с приложением

#### Создание каналов
- Нажмите **+** рядом с секцией каналов
- Введите название
- Выберите тип (текстовый/голосовой)

#### Отправка сообщений
- Выберите текстовый канал
- Введите сообщение
- Нажмите Enter

#### Отправка файлов
- Нажмите 📎 (скрепку)
- Выберите файл
- Файл автоматически загрузится

#### Голосовые звонки
- Нажмите на голосовой канал
- Используйте кнопки управления:
  - 🎤 Микрофон
  - 🔊 Звук
  - 📹 Камера

## Структура

```
client/
├── electron/          # Electron main process
│   ├── main.ts       # Главный процесс
│   └── preload.ts    # Preload скрипт
├── src/
│   ├── components/   # React компоненты
│   ├── api/          # API клиенты
│   ├── services/     # Сервисы (WebRTC)
│   ├── hooks/        # React hooks
│   ├── store/        # State management (Zustand)
│   ├── styles/       # CSS стили
│   └── main.tsx      # Точка входа React
├── index.html
└── package.json
```

## Технологии

- **Electron** - Десктоп платформа
- **React** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик
- **Zustand** - State management
- **Socket.io Client** - WebSocket
- **Axios** - HTTP клиент
- **WebRTC** - Голосовые/видео звонки

## Конфигурация

Приложение сохраняет настройки в localStorage:

- `fuse_token` - JWT токен
- `fuse_host` - Адрес сервера
- `fuse_port` - Порт сервера

## Сочетания клавиш

- `Enter` - Отправить сообщение
- `Esc` - Закрыть модальное окно

## WebRTC

Приложение использует WebRTC для P2P голосовых/видео звонков.

STUN серверы (по умолчанию):
- `stun:stun.l.google.com:19302`
- `stun:stun1.l.google.com:19302`

Для работы в интернете может потребоваться TURN сервер.

## Разрешения

Приложение запрашивает доступ к:
- 🎤 Микрофону
- 📹 Камере (опционально)

## Troubleshooting

### Не могу подключиться к серверу
- Проверьте, что сервер запущен
- Проверьте IP и порт
- Проверьте firewall

### Не работает микрофон
- Разрешите доступ к микрофону
- Проверьте настройки ОС
- Попробуйте другой браузер/перезапустите приложение

### Не слышу других пользователей
- Проверьте настройки звука
- Убедитесь, что вы не "заглушены"
- Проверьте, что другие пользователи не muted
