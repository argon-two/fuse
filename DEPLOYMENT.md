# 🚀 Руководство по развертыванию Fuse

Подробная инструкция по развертыванию Fuse в различных сценариях.

## 📋 Содержание

1. [Локальная сеть](#1-локальная-сеть)
2. [Через Hamachi VPN](#2-через-hamachi-vpn)
3. [Через Radmin VPN](#3-через-radmin-vpn)
4. [С белым IP](#4-с-белым-ip)
5. [На VPS/Выделенном сервере](#5-на-vpsвыделенном-сервере)

---

## 1. Локальная сеть

Самый простой вариант для использования дома или в офисе.

### Шаг 1: Подготовка сервера

На компьютере, который будет сервером:

```bash
# Установите PostgreSQL
# Windows: https://www.postgresql.org/download/windows/
# Linux: sudo apt install postgresql

# Создайте базу данных
createdb fuse

# Клонируйте проект
cd server
npm install
cp .env.example .env

# Отредактируйте .env
nano .env
```

### Шаг 2: Узнайте локальный IP

**Windows:**
```bash
ipconfig
# Ищите "IPv4 Address" - это будет что-то вроде 192.168.1.100
```

**Linux/Mac:**
```bash
ifconfig
# или
ip addr show
```

### Шаг 3: Запустите сервер

```bash
npm run build
npm start
```

Сервер запущен на `http://[ваш-локальный-ip]:3000`

### Шаг 4: Подключите клиентов

На всех компьютерах в той же сети:

1. Запустите клиент
2. Введите IP сервера (например, `192.168.1.100`)
3. Введите порт `3000`
4. Нажмите "Подключиться"

✅ **Готово!** Все в локальной сети могут пользоваться Fuse.

---

## 2. Через Hamachi VPN

Для игры через интернет без белого IP.

### Шаг 1: Установите Hamachi

1. Скачайте с https://www.vpn.net/
2. Установите на все компьютеры
3. Создайте аккаунт

### Шаг 2: Создайте сеть

На компьютере-сервере:

1. Откройте Hamachi
2. Нажмите "Создать новую сеть"
3. Введите:
   - Имя сети: `fuse-server`
   - Пароль: `ваш-пароль`
4. Запомните или запишите эти данные

### Шаг 3: Узнайте Hamachi IP

В Hamachi вы увидите IP вида `25.x.x.x` - это ваш Hamachi IP.

### Шаг 4: Запустите сервер Fuse

```bash
cd server
npm start
```

### Шаг 5: Подключите участников

На остальных компьютерах:

1. Откройте Hamachi
2. Нажмите "Подключиться к существующей сети"
3. Введите имя сети и пароль
4. Запустите клиент Fuse
5. Введите Hamachi IP сервера (например, `25.123.45.67`)
6. Порт: `3000`

✅ **Готово!** Все в Hamachi сети могут подключиться.

---

## 3. Через Radmin VPN

Альтернатива Hamachi, бесплатная и без ограничений.

### Шаг 1: Установите Radmin VPN

1. Скачайте с https://www.radmin-vpn.com/
2. Установите на все компьютеры
3. **Бесплатно и без регистрации!**

### Шаг 2: Создайте сеть

На компьютере-сервере:

1. Откройте Radmin VPN
2. Нажмите "Создать сеть"
3. Введите:
   - Имя сети: `FuseServer`
   - Пароль: `ваш-пароль`

### Шаг 3: Узнайте Radmin IP

В Radmin VPN вы увидите ваш IP (обычно начинается с `26.x.x.x`).

### Шаг 4: Запустите сервер

```bash
cd server
npm start
```

### Шаг 5: Подключите участников

На остальных компьютерах:

1. Откройте Radmin VPN
2. Нажмите "Присоединиться к сети"
3. Введите имя сети и пароль
4. Запустите клиент Fuse
5. Введите Radmin IP сервера
6. Порт: `3000`

✅ **Готово!** Неограниченное количество пользователей.

---

## 4. С белым IP

Если у вас есть белый (внешний) IP адрес.

### Шаг 1: Узнайте внешний IP

Перейдите на https://whatismyip.com/ с компьютера-сервера.

### Шаг 2: Настройте роутер (проброс портов)

1. Войдите в админ-панель роутера (обычно `192.168.1.1`)
2. Найдите раздел "Port Forwarding" или "Проброс портов"
3. Создайте правило:
   - **External Port**: 3000
   - **Internal Port**: 3000
   - **Internal IP**: локальный IP сервера
   - **Protocol**: TCP

### Шаг 3: Настройте Firewall

**Windows:**
```bash
# Откройте порт в Windows Firewall
netsh advfirewall firewall add rule name="Fuse Server" dir=in action=allow protocol=TCP localport=3000
```

**Linux:**
```bash
sudo ufw allow 3000/tcp
```

### Шаг 4: Запустите сервер

```bash
cd server
npm start
```

### Шаг 5: Подключайте пользователей

Пользователи могут подключаться из любой точки мира:

1. IP: ваш внешний IP (с whatismyip.com)
2. Порт: `3000`

✅ **Готово!** Глобальный доступ.

---

## 5. На VPS/Выделенном сервере

Профессиональное развертывание в облаке.

### Шаг 1: Получите VPS

Рекомендуемые провайдеры:
- DigitalOcean
- Linode
- AWS EC2
- Hetzner

Минимальные требования:
- 1 CPU
- 1 GB RAM
- 20 GB SSD
- Ubuntu 20.04 или выше

### Шаг 2: Подключитесь к серверу

```bash
ssh root@your-server-ip
```

### Шаг 3: Установите зависимости

```bash
# Обновите систему
apt update && apt upgrade -y

# Установите Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# Установите PostgreSQL
apt install -y postgresql postgresql-contrib

# Установите Nginx (опционально, для reverse proxy)
apt install -y nginx
```

### Шаг 4: Настройте PostgreSQL

```bash
# Переключитесь на пользователя postgres
sudo -u postgres psql

# В psql:
CREATE DATABASE fuse;
CREATE USER fuseuser WITH PASSWORD 'secure_password';
GRANT ALL PRIVILEGES ON DATABASE fuse TO fuseuser;
\q
```

### Шаг 5: Разверните приложение

```bash
# Клонируйте проект
cd /opt
git clone <your-repo-url> fuse
cd fuse/server

# Установите зависимости
npm install

# Настройте .env
cp .env.example .env
nano .env
```

Отредактируйте `.env`:
```env
PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fuse
DB_USER=fuseuser
DB_PASSWORD=secure_password
JWT_SECRET=generate-strong-secret-here
```

### Шаг 6: Соберите и запустите

```bash
npm run build
npm start
```

### Шаг 7: Настройте PM2 (автозапуск)

```bash
# Установите PM2
npm install -g pm2

# Запустите приложение
pm2 start dist/index.js --name fuse-server

# Автозапуск при перезагрузке
pm2 startup
pm2 save
```

### Шаг 8: Настройте Nginx (опционально)

```bash
nano /etc/nginx/sites-available/fuse
```

Содержимое:
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Активируйте:
```bash
ln -s /etc/nginx/sites-available/fuse /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

### Шаг 9: SSL (опционально, но рекомендуется)

```bash
# Установите Certbot
apt install -y certbot python3-certbot-nginx

# Получите сертификат
certbot --nginx -d your-domain.com
```

### Шаг 10: Firewall

```bash
ufw allow 22/tcp    # SSH
ufw allow 80/tcp    # HTTP
ufw allow 443/tcp   # HTTPS
ufw allow 3000/tcp  # Fuse (если без Nginx)
ufw enable
```

✅ **Готово!** Ваш сервер работает 24/7 в облаке.

---

## 🔧 Устранение неполадок

### Не могу подключиться к серверу

1. **Проверьте, что сервер запущен**
   ```bash
   # На сервере
   curl http://localhost:3000/health
   ```

2. **Проверьте firewall**
   ```bash
   # Windows
   netsh advfirewall show allprofiles
   
   # Linux
   sudo ufw status
   ```

3. **Проверьте, что порт открыт**
   ```bash
   # На клиенте
   telnet server-ip 3000
   # или
   nc -zv server-ip 3000
   ```

### PostgreSQL ошибки

```bash
# Проверьте статус
sudo systemctl status postgresql

# Перезапустите
sudo systemctl restart postgresql

# Проверьте логи
sudo tail -f /var/log/postgresql/postgresql-*-main.log
```

### Проблемы с WebRTC (нет звука)

1. Проверьте разрешения микрофона/камеры
2. Используйте HTTPS (для продакшна)
3. Настройте TURN сервер для работы через NAT

---

## 📊 Мониторинг

### Логи PM2

```bash
pm2 logs fuse-server
pm2 monit
```

### Статус сервера

```bash
# Использование ресурсов
htop

# Дисковое пространство
df -h

# Сетевые подключения
netstat -tulpn | grep 3000
```

---

## 🔄 Обновление

### На VPS

```bash
cd /opt/fuse
git pull
cd server
npm install
npm run build
pm2 restart fuse-server
```

### Локально

```bash
cd server
git pull
npm install
npm run build
npm start
```

---

## 🔐 Безопасность

1. **Измените JWT_SECRET** в `.env`
2. **Используйте сильные пароли** для БД
3. **Настройте SSL** для продакшна
4. **Ограничьте доступ** к PostgreSQL
5. **Регулярно обновляйте** зависимости

```bash
npm audit
npm audit fix
```

---

<div align="center">

**Удачного развертывания! 🚀**

Если возникли вопросы - создайте Issue на GitHub.

</div>
